extends: base.yaml
name: "Child Config"
# Override parent's max_combinations
max_combinations: 5000

design_space:
  transforms:
    optimize:
      - FoldConstants
  kernels:
    - MatrixVectorActivation: mvau_hls
build_pipeline:
  steps:
    - "{cleanup}"
    - "{optimize}"