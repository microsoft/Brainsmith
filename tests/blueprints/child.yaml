extends: base.yaml
name: "Child Config"
# Override parent's max_combinations
max_combinations: 5000

design_space:
  steps:
    - "cleanup"
    - ["optimize_minimal", "optimize_aggressive"]
  kernels:
    - MatrixVectorActivation: mvau_hls