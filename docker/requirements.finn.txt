# install PyTorch
torch==2.7.0
torchvision==0.22.0
torchaudio==2.7.0 --extra-index-url https://download.pytorch.org/whl/cu121
# extra Python package dependencies (for testing and interaction)
pygments==2.14.0
ipykernel==6.21.2
markupsafe==2.0.1
matplotlib==3.7.0
pytest-dependency==0.5.1
pytest-xdist[setproctitle]==3.2.0
pytest-parallel==0.1.1
netron>=5.0.0
pandas==1.5.3
scikit-learn==1.2.1
tqdm==4.64.1
-e git+https://github.com/fbcotter/dataset_loading.git@0.0.4#egg=dataset_loading
# RTL simulation dependencies for FINN - not needed for transformer branch
# -e git+https://github.com/maltanar/pyverilator.git@master#egg=pyverilator
# these versions of pytest and associated plugins allow for stable collection of
# test reports and code coverage reports in HTML
pytest==6.2.5
pytest-metadata==1.7.0
pytest-html==3.0.0
pytest-html-merger==0.0.8
pytest-cov==4.1.0
# extra dependencies from other deps
# installed in Docker image to make entrypoint script go faster
# finn-experimental
deap==1.3.1
mip==1.13.0
networkx==2.8
# brevitas
future-annotations==1.0.0
dependencies==2.0.1
tokenize-rt==4.2.1
# assure that we have the right setuptools version
setuptools==68.2.2
